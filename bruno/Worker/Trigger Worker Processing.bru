meta {
  name: Trigger Worker Processing
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/worker/process
}

docs {
  Manually trigger the worker to process any queued runs.
  
  Useful for testing or forcing immediate processing without waiting
  for the automatic check interval.
  
  Returns:
    - message: "Processing triggered"
}
